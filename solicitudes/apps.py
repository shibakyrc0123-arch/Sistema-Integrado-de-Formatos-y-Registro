from django.apps import AppConfig
from django.apps import AppConfig

class SolicitudesConfig(AppConfig):
    name = 'solicitudes'

class SolicitudesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    # Comentario: Este nombre debe ser exactamente 'solicitudes'
    name = 'solicitudes'